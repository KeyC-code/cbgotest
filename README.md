Mock CBR (Центральный Банк России) Service
Мок-сервис для имитации API Центрального Банка России. Позволяет тестировать приложения, которые зависят от курсов валют ЦБ, без необходимости обращения к реальному API.

🚀 Возможности
Имитация API ЦБ: Поддерживает те же эндпоинты и формат данных, что и реальный ЦБ

Управление курсами валют: Динамическое изменение курсов валют во время выполнения

Сервис оплаты: Встроенный сервис валидации платежей с лимитами

Тестирование: Готовые тесты для проверки функциональности

Параллельная обработка: Поддержка параллельных запросов

📁 Структура проекта
text
mock-cbr/
├── main.go                 # Точка входа, запуск сервера
├── internal/
│   ├── handler/           # Обработчики HTTP-запросов
│   │   ├── cbr_handler.go
│   │   └── cbr_handler_test.go
│   ├── models/           # Модели данных
│   │   └── cbr_models.go
│   ├── storage/          # Хранилище курсов валют
│   │   └── rates.go
│   └── service/          # Бизнес-логика (сервис оплаты)
│       └── payment.go
├── payment_test.go       # Тесты сервиса оплаты
└── go.mod               # Зависимости Go
🛠 Установка и запуск
Предварительные требования
Go 1.25.3 или выше

Установка
bash
# Клонирование репозитория
git clone <repository-url>
cd mock-cbr

# Сборка проекта
go build -o mock-cbr main.go
Запуск
bash
# Запуск сервера
./mock-cbr

# Или напрямую через Go
go run main.go
Сервер запустится на порту 8080:

text
Мок сервис ЦБ запущен на localhost:8080
Доступные эндпоинты:
  - http://localhost:8080/scripts/XML_daily.asp
  - http://localhost:8080/cbr
🔌 API Эндпоинты
Получение курсов валют
bash
# Базовый запрос (текущие курсы)
curl http://localhost:8080/cbr

# С указанием даты (формат dd/mm/yyyy)
curl "http://localhost:8080/cbr?date_req=02/03/2002"

# Использование оригинального эндпоинта ЦБ
curl http://localhost:8080/scripts/XML_daily.asp
Ответ API
Сервер возвращает XML в формате ЦБ:

xml
<ValCurs Date="02.03.2024" name="Foreign Currency Market">
  <Valute ID="R01235">
    <NumCode>840</NumCode>
    <CharCode>USD</CharCode>
    <Nominal>1</Nominal>
    <Name>Доллар США</Name>
    <Value>90,0000</Value>
  </Valute>
  <!-- Другие валюты -->
</ValCurs>
💰 Сервис оплаты
Сервис проверяет платежи с учетом лимитов (по умолчанию 15000 RUB).

Использование
go
import "mock-cbr/internal/service"

rates := storage.NewRates()
paymentService := service.NewPaymentService(rates)

request := service.PaymentRequest{
    Provider: "test_provider",
    Amount:   100,
    Currency: "USD",
}

result := paymentService.ProcessPayment(request)
Логика валидации:
Проверка существования валюты

Конвертация суммы в рубли

Проверка лимитов:

Максимальный лимит: 15000 RUB

Сумма должна быть положительной

⚙️ Конфигурация
Начальные курсы валют
По умолчанию установлены:

USD: 90.0

EUR: 98.5

YEN: 0.5

Поддерживаемые валюты
Сервис включает маппинг для 16 валют:

USD (Доллар США)

EUR (Евро)

GBP (Фунт стерлингов)

JPY (Иена)

И другие

Изменение курсов в runtime
go
rates := storage.NewRates()
rates.SetRate("USD", 95.5)  // Установить курс USD
rates.SetRate("EUR", 105.0) // Установить курс EUR
🧪 Тестирование
Запуск всех тестов
bash
go test ./...
Тесты включают:
Тест API ЦБ: Проверка корректности XML-ответа

Тест сервиса оплаты:

Платежи в пределах лимита

Превышение лимита

Неподдерживаемые валюты

Отрицательные суммы

Параллельная обработка: Тестирование конкурентных запросов

Пример теста
bash
# Запуск теста сервиса оплаты
go test -v payment_test.go

# Запуск теста обработчика
go test -v ./internal/handler/
📊 Примеры использования
Тестирование интеграции с API ЦБ
bash
# Получение курсов валют
curl http://localhost:8080/cbr | xmllint --format -

# Просмотр структуры ответа
curl -s http://localhost:8080/cbr | grep -E "(CharCode|Value)"
